# Lessons

- Proget's website is `https://proget.pl`, not `proget.online`. Don't guess vendor URLs: verify or ask.
- This is a published package: any functional change that gets committed must include a version bump in `package.json`, or the release/update will fail. Bump before pushing, not as an afterthought.
